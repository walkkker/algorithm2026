# Hot 100 堆逐题详解

> 本文按项目现有package整理。`【Hot 100】`是主线题，`【扩展】`是同模型扩展题。

## 专题核心

堆只维护当前最需要被访问或淘汰的极值，适合Top K和动态中位数。

**统一记忆：** Top K用大小为K的门槛堆；中位数用左大根堆和右小根堆并维护规模差不超过1。

## 题目清单

- Q215 KthLargestElementInAnArray `【Hot 100】`
- Q295 FindMedianFromDataStream `【Hot 100】`
- Q347 TopKFrequentElements `【Hot 100】`

---

## Q215 KthLargestElementInAnArray 【Hot 100】

> **重要题型识别：无序数组第k大／第k小 → Quickselect。**
> 独立专题：[Quickselect快速选择：第K大与第K小](./Quickselect快速选择_第K大与第K小.md)。
> 荷兰国旗分区 + 类二分淘汰，随机化后期望O(N)、最坏O(N²)；大小为k的堆则为最坏O(N log(k+1))。

### 核心思想与题目要求

215. 数组中的第K个最大元素
TODO: 【原理解修正】“每次舍弃一半”只是理想情况；随机Quickselect仅继续处理一侧，期望O(N)，最坏O(N²)。
给定整数数组`nums`和整数`k`，返回数组中第`k`个最大的元素。
需要查找的是数组排序后的第`k`个最大元素，而不是第`k`个不同的元素。

本题核心算法是 QuickSelect: 实现上就是 类似二分 + partition。  神似快排，但是因为扫描的数据量不一样，所以平均时间复杂度为O(N)，最差退化到O(N^2)

### 2026-09-19复盘：为什么第K大可以使用荷兰国旗

荷兰国旗分区会把当前区间划分为：

~~~text
[小于pivot][等于pivot][大于pivot]
~~~

虽然三个区域内部没有完全排序，但`等于pivot`区域在完整升序数组中的最终下标范围已经确定。
因此，只要先把第k大转换为升序目标下标：

~~~java
int targetIndex = nums.length - k;
~~~

就可以根据目标下标与等于区间的位置关系淘汰区域：

~~~text
targetIndex < equalLeft   -> 只保留左侧
targetIndex > equalRight  -> 只保留右侧
否则                     -> pivot就是答案
~~~

这就是“荷兰国旗 + 类二分区间淘汰”的Quickselect。它不像普通二分那样依赖原数组有序，
而是先通过一次partition制造出能够安全排除区间的信息。

### 两个基础错误

闭区间初始化：

~~~java
int left = 0;
int right = nums.length - 1;
~~~

`right`是可访问的数组下标，不能写成`nums.length`。

第k大不是下标k：

~~~text
升序数组长度为N
最大值下标       N-1，对应第1大
次大值下标       N-2，对应第2大
第k大目标下标    N-k
~~~

partition返回的是升序排列语义下的最终区间，所以后续必须比较`targetIndex`，不能直接比较题目
给出的1-based变量`k`。

### Quickselect为什么期望O(N)

需要修正一个不严谨说法：随机pivot不保证每轮都能去掉一半。只有pivot恰好接近中间时，剩余
规模才接近`N/2`。随机化保证的是长期来看不容易持续选中极端pivot，剩余规模在期望意义上
按固定比例缩小。

Quickselect每轮只继续处理目标所在的一侧，因此扫描量近似为：

~~~text
N + aN + a^2N + a^3N + ... = O(N)，其中0 < a < 1
~~~

如果为了理解暂时假设每次正好减半，则是：

~~~text
N + N/2 + N/4 + ... < 2N
~~~

这能解释几何级数，但“每次减半”不是算法保证，只是便于理解的理想情况。

### 与快速排序的区别

| 算法 | partition后继续处理 | 期望复杂度 |
| --- | --- | --- |
| QuickSort | 左右两侧都处理 | `O(N log N)` |
| Quickselect | 只处理目标所在一侧 | `O(N)` |

快速排序在同一递归层需要处理所有分区，总工作量约为`O(N)`，期望高度为`O(log N)`。
Quickselect只沿一条路径继续，后续扫描区间越来越小，所以期望总扫描量为`O(N)`。

但随机Quickselect最坏仍可能连续选到端点pivot：

~~~text
N + (N-1) + (N-2) + ... = O(N^2)
~~~

因此准确复杂度是：期望`O(N)`，最坏`O(N^2)`，额外空间在迭代实现中为`O(1)`。

### 两套荷兰国旗边界定义

第一套把`less`和`more`定义为开区间端点：

~~~text
[left, less)  < pivot
[less, index) == pivot
[index, more] 待处理
(more, right] > pivot
~~~

循环条件是`index <= more`，结束后等于区间直接是`[less,more]`。

第二套把pivot预留在`right`位置：

~~~text
[left, less]  < pivot
(less, index) == pivot
[index, more) 待处理
[more, right) > pivot
right          pivot
~~~

循环条件是`index < more`，结束后必须执行：

~~~java
swap(nums, more, right);
~~~

再返回等于区间`[less + 1,more]`。两套定义都正确，但循环条件、边界含义、最终交换和返回值
必须成套使用，不能混搭。

### 算法流程与正确性依据

1. 根据题意确定输入中可利用的单调性、值域、结构关系或可复用子问题。
2. 使用本专题模型组织状态：Top K用大小为K的门槛堆；中位数用左大根堆和右小根堆并维护规模差不超过1。
3. 每次更新后维持上述定义；只有在不变量仍成立时才移动指针、覆盖数据或进入下一状态。
4. 最终从定义直接读取答案，而不是根据样例猜测返回位置。

正确性的关键是：每一步只排除已经证明不可能成为答案的选择，或者完整枚举状态定义允许的所有前驱，因此不会漏解。

### 标准 Java 代码

```java
package frequence.Heap;

/**
 * 215. 数组中的第K个最大元素
 *
 * TODO: Quickselect只处理目标所在一侧，随机化后期望O(N)、最坏O(N^2)。
 * TODO: T(N)=T(N/2)+O(N)只是理想减半模型，并非每轮的保证。
 *
 * <p>给定整数数组{@code nums}和整数{@code k}，返回数组中第{@code k}个最大的元素。
 *
 * <p>需要查找的是数组排序后的第{@code k}个最大元素，而不是第{@code k}个不同的元素。
 */
public class Q215_KthLargestElementInAnArray {

    /**
     本题核心算法是 QuickSelect: 实现上就是 类似二分 + partition。  神似快排，但是因为扫描的数据量不一样，所以平均时间复杂度为O(N)，最差退化到O(N^2)
     */
    class Solution {
        public int findKthLargest(int[] nums, int k) {
            int targetIndex = nums.length - k;
            int l = 0;
            int r = nums.length - 1;
            while (l <= r) {
                int[] p = partition(nums, l, r);
                // TODO: 【错误】你这里要用 targetIndex去比较了。  因为targetIndex才对应 升序数组。
                // if (k >= p[0] && k <= p[1]) {
                //     return nums[p[0]];
                // } else if (k < p[0]) {
                //     r = p[0] - 1;
                // } else {
                //     l = p[1] + 1;
                // }
                if (targetIndex >= p[0] && targetIndex <= p[1]) {
                    return nums[targetIndex];
                } else if (targetIndex < p[0]) {
                    r = p[0] - 1;
                } else {
                    l = p[1] + 1;
                }
            }
            return 0;
        }

        public int[] partition(int[] arr, int l, int r) {
            int pivotIndex = l + (int) (Math.random() * (r - l + 1));
            int pivot = arr[pivotIndex];
            int less = l - 1;
            int more = r + 1;
            int index = l;
            while (index < more) {
                if (arr[index] == pivot) {
                    index++;
                } else if (arr[index] < pivot) {
                    swap(arr, index++, ++less);
                } else {
                    swap(arr, index, --more);
                }
            }
            return new int[]{less + 1 , more - 1};
        }

        public void swap(int[] arr, int i, int j) {
            int tmp = arr[i];
            arr[i] = arr[j];
            arr[j] = tmp;
        }
    }}
```

### 易错点

- TODO: 无序数组第k大／第k小，应想到Quickselect。
- TODO: 随机pivot不保证每轮折半，必须区分期望O(N)与最坏O(N²)。
- TODO: 【错误】你这里要用 targetIndex去比较了。  因为targetIndex才对应 升序数组。

### 复杂度与面试记忆

- 理想减半时扫描量为N+N/2+N/4+...；随机化下需按期望分析，不能保证每轮减半。
- 本题核心算法是 QuickSelect: 实现上就是 类似二分 + partition。  神似快排，但是因为扫描的数据量不一样，所以平均时间复杂度为O(N)，最差退化到O(N^2)

> 记忆模板：Top K用大小为K的门槛堆；中位数用左大根堆和右小根堆并维护规模差不超过1。

### 项目文件

- [主实现](Q215_KthLargestElementInAnArray.java)

---

## Q295 FindMedianFromDataStream 【Hot 100】

### 2026-09-19复盘：命名与跨堆不变量

对应[Q295代码](./Q295_FindMedianFromDataStream.java)最前面的`MedianFinderReviewed20260919`。
本次保留你的upper/lower命名及代码逻辑，当前实现正确：插入O(log N)、查询O(1)、空间O(N)。

你的原始记忆：“纯寻找中位数，无删除，双堆战法；线性顺序是upper | lower。”

| 实际职责 | 本次用户版本 | 下方固定步骤版本 |
| --- | --- | --- |
| 左侧较小的一半，大根堆 | upper | lower |
| 右侧较大的一半，小根堆 | lower | upper |

两套实现的名字正好相反，但维护同样的两个不变量：

```text
左侧所有元素 <= 右侧所有元素
左侧数量 == 右侧数量，或者左侧数量 == 右侧数量 + 1
```

**误区：“左侧大根堆，所以左侧所有元素 >= 右侧所有元素。”**
大根堆只保证堆顶是本堆内部最大值；跨堆大小关系由分区和转移操作维护。

```text
左侧upper（大根堆）   | 右侧lower（小根堆）
       {1,2,3}       |       {4,5}
        堆顶3                 堆顶4
```

这里集合仅展示成员，不表示堆内部已经完整排序。左侧最大值3仍小于右侧最小值4。
大根堆方便读取较小一半的最大值，小根堆方便读取较大一半的最小值，中位数就在这两个边界上。

你的代码固定步骤为：

```java
upper.offer(num);                 // 先入左侧大根堆
lower.offer(upper.poll());        // 左侧最大值转入右侧
if (lower.size() > upper.size()) {
    upper.offer(lower.poll());    // 右侧最小值移回左侧，保证左侧不比右侧少
}
```

原注释把“先入lower”标为错误，需要限定为“不符合本次变量定义和左侧多一个的约定”。
在下面的FixedStepMedianFinder中，lower就是左侧大根堆，先入lower完全正确。
复习时先看比较器和分区职责，再看变量名，不能仅凭upper/lower字面意思判断大小关系。

### 核心思想与题目要求

295. 数据流的中位数
中位数是有序整数列表中间位置的值。如果列表长度为偶数，中位数是中间两个值的平均值。
设计一个支持以下操作的数据结构：
    - `addNum(num)`：将整数加入数据流；
    - `findMedian()`：返回当前所有元素的中位数。
**核心思路：**`more`是保存左半区的大根堆，`less`是保存右半区的小根堆。
始终维持`more.size() == less.size()`或`more.size() == less.size() + 1`，并保证
`more.peek() <= less.peek()`。因此奇数个元素时中位数是`more.peek()`，偶数个元素时
是两个堆顶的平均值。
`addNum`不能只按堆大小直接加入某个堆，因为这只能维持容量，不能保证左右区间有序。
当前实现先加入一侧，再把该侧堆顶转移到另一侧，一次操作同时维护两个不变量。

### 2026-09-14复盘：双堆与固定三步

把数据流概念上切成两半：

~~~text
较小的一半                         较大的一半
[...... lower.peek()] | [upper.peek() ......]
          左侧最大值       右侧最小值
~~~

- `lower`保存较小的一半，必须使用大根堆；
- `upper`保存较大的一半，使用Java `PriorityQueue`默认的小根堆。

需要同时维护两个不变量：

~~~text
顺序不变量：lower中的所有元素 <= upper中的所有元素

数量不变量：lower.size() == upper.size()
          或lower.size() == upper.size() + 1
~~~

只按照两个堆的大小直接放入新元素，只能维护数量关系，不能保证新元素落在正确的数值区间。
最稳定的写法是不判断新元素属于哪边，每次固定执行三步：

~~~java
// 1. 新元素先进入左侧大根堆。
lower.offer(num);

// 2. 左堆最大值转入右侧小根堆，维护顺序不变量。
upper.offer(lower.poll());

// 3. 如果右侧数量更多，把右侧最小值移回左侧，维护数量不变量。
if (upper.size() > lower.size()) {
    lower.offer(upper.poll());
}
~~~

第2步执行后，`lower`中留下的都是更小的元素；被转移的是`lower`当前最大值，因此能够进入
保存较大一半的`upper`。第3步只负责让奇数个元素时`lower`多一个。

中位数由数量不变量直接得到：

~~~java
if (lower.size() > upper.size()) {
    return lower.peek();
}
return ((double) lower.peek() + upper.peek()) / 2.0;
~~~

### PriorityQueue默认方向

~~~java
PriorityQueue<Integer> upper = new PriorityQueue<>();
// 默认自然顺序，小根堆，peek()返回最小值。

PriorityQueue<Integer> lower =
        new PriorityQueue<>(Comparator.reverseOrder());
// 反向顺序，大根堆，peek()返回最大值。
~~~

不推荐使用`(a,b) -> b-a`，因为整数极值相减可能溢出。双堆插入时间复杂度为`O(log N)`，
查询中位数为`O(1)`，总空间复杂度为`O(N)`。

### 算法流程与正确性依据

1. 根据题意确定输入中可利用的单调性、值域、结构关系或可复用子问题。
2. 使用本专题模型组织状态：Top K用大小为K的门槛堆；中位数用左大根堆和右小根堆并维护规模差不超过1。
3. 每次更新后维持上述定义；只有在不变量仍成立时才移动指针、覆盖数据或进入下一状态。
4. 最终从定义直接读取答案，而不是根据样例猜测返回位置。

正确性的关键是：每一步只排除已经证明不可能成为答案的选择，或者完整枚举状态定义允许的所有前驱，因此不会漏解。

### 标准 Java 代码

```java
package frequence.Heap;

import java.util.PriorityQueue;

/**
 * 295. 数据流的中位数
 *
 * <p>中位数是有序整数列表中间位置的值。如果列表长度为偶数，中位数是中间两个值的平均值。
 *
 * <p>设计一个支持以下操作的数据结构：
 * <ul>
 *     <li>{@code addNum(num)}：将整数加入数据流；</li>
 *     <li>{@code findMedian()}：返回当前所有元素的中位数。</li>
 * </ul>
 *
 * <p><b>核心思路：</b>{@code more}是保存左半区的大根堆，{@code less}是保存右半区的小根堆。
 * 始终维持{@code more.size() == less.size()}或{@code more.size() == less.size() + 1}，并保证
 * {@code more.peek() <= less.peek()}。因此奇数个元素时中位数是{@code more.peek()}，偶数个元素时
 * 是两个堆顶的平均值。
 *
 * <p>{@code addNum}不能只按堆大小直接加入某个堆，因为这只能维持容量，不能保证左右区间有序。
 * 当前实现先加入一侧，再把该侧堆顶转移到另一侧，一次操作同时维护两个不变量。
 */
public class Q295_FindMedianFromDataStream {

    public static class MedianFinder {

        private final PriorityQueue<Integer> more;
        private final PriorityQueue<Integer> less;
        private int size;

        public MedianFinder() {
            more = new PriorityQueue<>((a, b) -> b - a);
            less = new PriorityQueue<>();
            size = 0;
        }

        public void addNum(int num) {
            if (more.size() == less.size() + 1) {
                more.offer(num);
                less.offer(more.poll());
            } else {
                less.offer(num);
                more.offer(less.poll());
            }
            size++;
        }

        public double findMedian() {
            if (size % 2 == 1) {
                return (double) more.peek();
            } else {
                return ((double) more.peek() + less.peek()) / 2;
            }
        }
    }
}
```

### 易错点

- 不要在没有写清状态、区间或返回值定义前直接套模板。
- 边界条件必须与主循环使用同一套下标语义。

### 复杂度与面试记忆

复杂度需要按照代码中每个指针、节点、状态或边的实际访问次数推导；不能仅根据算法名称下结论。

> 记忆模板：Top K用大小为K的门槛堆；中位数用左大根堆和右小根堆并维护规模差不超过1。

### 项目文件

- [主实现](Q295_FindMedianFromDataStream.java)

---

## Q347 TopKFrequentElements 【Hot 100】

### 2026-09-19复盘：频次统计 + 分桶法

对应[Q347代码](./Q347_TopKFrequentElements.java)最前面的`SolutionReviewed20260919`。
你的实现正确，按哈希表操作期望O(1)分析，总时间期望O(N)、额外空间O(N)，是时间渐进最优解之一。

> **桶下标表示频次，桶内列表保存具有该频次的不同数字；从高频向低频收集，达到k个立即返回。**

为什么能分桶：虽然数字本身可能很大或为负数，但一个数字的出现频次一定在`[1,N]`，
其中N是实际输入长度。因此只需N+1个桶位置，与数字的值域无关。

```text
nums = [1,1,1,2,2,3], k = 2
频次：1->3次，2->2次，3->1次
buckets[3] = [1]
buckets[2] = [2]
buckets[1] = [3]
从后往前收集两个数字，得到[1,2]。
```

同一桶内的数字频次相同，无需再排序。桶数组初始位置为null，只对实际出现的频次创建列表。

#### 错误一：泛型数组创建

```java
// 编译错误：Java不允许直接创建参数化类型的数组。
// List<Integer>[] buckets = new List<>[nums.length + 1];
// List<Integer>[] buckets = new List<Integer>[nums.length + 1];

// 你的原写法可编译，但有unchecked conversion警告。
List<Integer>[] buckets = new List[nums.length + 1];
```

如需明确表达转换，并局部抑制警告：

```java
@SuppressWarnings("unchecked")
List<Integer>[] buckets =
        (List<Integer>[]) new List<?>[nums.length + 1];
```

`List<?>`属于可具体化类型，允许创建数组；但转换到`List<Integer>[]`仍无法被运行时完整验证。
上述用法依赖新数组最初全为null，后续始终只放入`List<Integer>`，且不经原始类型等可写别名污染。
它并没有消除未经检查的转换，只是明确记录并局部抑制警告。

完全避免这种转换可使用`List<List<Integer>>`，先`add(null)`建立N+1个位置，再用get/set访问。
`new ArrayList<>(N+1)`只设置容量，不会建立逻辑元素。

#### 错误二：收集够k个后不能只break

```java
ans[index++] = num;
if (index == k) {
    // 错误行：break;
    return ans;
}
```

原注释将它归为“语法错误”，应纠正为**控制流逻辑错误**：break语法合法，只退出最内层for。
若外层随后找到另一个非空桶，就会写入`ans[k]`并越界。
例如`[1,1,2], k=1`，收集1后只break，后续还会尝试收集2。return则直接结束整个方法。

#### 复杂度与最优性

设U为不同数字个数，U<=N：

- 统计频次：期望O(N)。
- 初始化桶数组：O(N)；分桶：O(U)。
- 从后向前扫描：最多N+1个桶位置，所有桶内部合计最多U个数字，因此O(N+U)，不是O(N²)。
- 总时间：期望O(N)；总额外空间：O(N)。读取输入本身需要线性时间，因此达到时间渐进最优阶。

大小为k的堆最坏为O(N log(k+1))，通常写O(N log k)，k接近N时可能达到O(N log N)。
分桶利用频次值域有界，满足本题优于O(N log N)的要求，但不意味着它在所有场景下都最节省空间。

#### 其他边界检查

- 桶长度必须为N+1，某个数字可能出现N次，需要访问`buckets[N]`。
- 外层`i>=0`正确，桶0为空；写成`i>=1`只是一处可选精简。
- 当前实现使用题目保证的`1<=k<=U`，不把k=0或k>U当成合法输入。

### 核心思想与题目要求

347. 前K个高频元素
给定一个整数数组`nums`和一个整数`k`，返回其中出现频率最高的`k`个
元素。答案可以按任意顺序返回。
要求算法的时间复杂度优于`O(N log N)`。

1. 对应本题是：HashMap + 小根堆（门槛堆）
2. 分桶版已完成，见本节开头的2026-09-19复盘及源码`SolutionReviewed20260919`。
本答案是简化版本的 门槛堆-小根堆。
 1. 先把正确版本写出来并讲清复杂度。如果面试官继续追问常数优化，再说：
    - 当前实现为了模板简洁，采用先入堆再淘汰。还可以把堆顶作为准入门槛，只有新元素频率更高时才执行替换，从而【减少不必要的堆调整】。

下面这个是很早之前我写的版本，门槛堆的常数时间更小。

哈希表词频统计<int num, Node > + 封装Node（num + freq）+  门槛堆
【难点】在于 封装 num + frep 在一起。从而从门槛堆中 既控制了频次，又能够从最终size中获得对应num的值。
【步骤】
使用 HashMap 统计完词频后，遍历每一个values。 放入到门槛堆。
门槛堆是一个小根堆，
（1）if heap.size() < targetSize，那么就 入堆
（2）if size满了 && 当前元素 比 堆顶大的话，那么就要 堆顶出 + 新元素进
通过上面两步，实现了门槛堆（小根堆），最终 只剩 前 size 个 最大的出现频率 的数字
PS： 但是你看到了，在门槛堆中，我们比较的是频次，但是最终要拿出来的结果 是 该频次对应的值。 这就使得 同一个对象需要具备两个属性： 【1】频次用来比较 【2】对应的值用来获取结果  ==》 所以要封装成Node实例就可以了
-----------------------------------------------------------------------
利用一个堆就可以了 -> 门槛堆
- 门槛堆
- 就是 小根堆组织（堆顶就是这个门槛）， 当前的数字 能不能将 门槛干掉 自己进来。（有点类似于 堆实现 的 topK）
- 先进行 词频统计（词频表）
- 准备一个小根堆 （次数 少的 放在顶部）
- 看小根堆 有没有满

### 算法流程与正确性依据

1. 根据题意确定输入中可利用的单调性、值域、结构关系或可复用子问题。
2. 使用本专题模型组织状态：Top K用大小为K的门槛堆；中位数用左大根堆和右小根堆并维护规模差不超过1。
3. 每次更新后维持上述定义；只有在不变量仍成立时才移动指针、覆盖数据或进入下一状态。
4. 最终从定义直接读取答案，而不是根据样例猜测返回位置。

正确性的关键是：每一步只排除已经证明不可能成为答案的选择，或者完整枚举状态定义允许的所有前驱，因此不会漏解。

### 标准 Java 代码

```java
package frequence.Heap;

import java.util.*;

/**
 * 347. 前K个高频元素
 *
 * <p>给定一个整数数组{@code nums}和一个整数{@code k}，返回其中出现频率最高的{@code k}个
 * 元素。答案可以按任意顺序返回。
 *
 * <p>要求算法的时间复杂度优于{@code O(N log N)}。
 */
public class Q347_TopKFrequentElements {

    /**
     * 1. 对应本题是：HashMap + 小根堆（门槛堆）
     * 2. 最优解是 桶（后面TODO）
     *
     * 本答案是简化版本的 门槛堆-小根堆。
     *  1. 先把正确版本写出来并讲清复杂度。如果面试官继续追问常数优化，再说：
     *     - 当前实现为了模板简洁，采用先入堆再淘汰。还可以把堆顶作为准入门槛，只有新元素频率更高时才执行替换，从而【减少不必要的堆调整】。
     */
    class Solution {
        public int[] topKFrequent(int[] nums, int k) {
            HashMap<Integer, Integer> map = new HashMap<>();
            for (int num : nums) {
                map.put(num, map.getOrDefault(num, 0) + 1);
            }

            // TODO: 这个PriorityQueue的 lambda太屌了。   泛型Integer对应下标（本题是数字本身，对应Map.key），lambda比较时，去map拿value。
            PriorityQueue<Integer> heap = new PriorityQueue<>((a, b) -> (map.get(a) - map.get(b)));


            // TODO: 这是门槛堆的写法，一定要记住。
            // TODO: 小根堆的堆顶是当前 K 个候选中频率最低的元素。当大小超过 K，就把它淘汰。
            for (int key : map.keySet()) {
                heap.add(key);

                if (heap.size() > k) {
                    heap.poll();
                }
            }

            int[] ans = new int[k];
            int index = 0;
            while (!heap.isEmpty()) {
                ans[index++] = heap.poll();
            }
            return ans;
        }
    }



    /**
     * 下面这个是很早之前我写的版本，门槛堆的常数时间更小。
     */
    /**
     哈希表词频统计<int num, Node > + 封装Node（num + freq）+  门槛堆
     【难点】在于 封装 num + frep 在一起。从而从门槛堆中 既控制了频次，又能够从最终size中获得对应num的值。

     【步骤】
     使用 HashMap 统计完词频后，遍历每一个values。 放入到门槛堆。
     门槛堆是一个小根堆，
     （1）if heap.size() < targetSize，那么就 入堆
     （2）if size满了 && 当前元素 比 堆顶大的话，那么就要 堆顶出 + 新元素进
     通过上面两步，实现了门槛堆（小根堆），最终 只剩 前 size 个 最大的出现频率 的数字
     PS： 但是你看到了，在门槛堆中，我们比较的是频次，但是最终要拿出来的结果 是 该频次对应的值。 这就使得 同一个对象需要具备两个属性： 【1】频次用来比较 【2】对应的值用来获取结果  ==》 所以要封装成Node实例就可以了
     -----------------------------------------------------------------------

     利用一个堆就可以了 -> 门槛堆
     - 门槛堆
     - 就是 小根堆组织（堆顶就是这个门槛）， 当前的数字 能不能将 门槛干掉 自己进来。（有点类似于 堆实现 的 topK）
     - 先进行 词频统计（词频表）
     - 准备一个小根堆 （次数 少的 放在顶部）
     - 看小根堆 有没有满
     */
    class Solution1 {
        public int[] topKFrequent(int[] nums, int k) {
            HashMap<Integer, Node> map = new HashMap<>();
            // Step1 统计词频
            for (int i = 0; i < nums.length; i++) {
                if (!map.containsKey(nums[i])) {
                    map.put(nums[i], new Node(nums[i], 0));
                }
                // 【错误点】这里不是 重新赋值。 而是直接访问对应Node，修改Node里面的值
                // map.put(nums[i], map.get(nums[i]).freq + 1);
                map.get(nums[i]).freq++;
            }


            PriorityQueue<Node> minHeap = new PriorityQueue<>((a,b) -> (a.freq - b.freq)); // 注意，因为对象不可比较，所以要传入比较器
            // 词频统计完成之后，都封装到了Node里面，此时遍历Node， 使用 map.values()
            for (Node node : map.values()) {
                if (minHeap.size() < k) {                    // 门槛堆未满时，直接加入
                    minHeap.add(node);
                } else {
                    if (node.freq > minHeap.peek().freq) {  // 门槛堆满了，只有当堆顶元素<新元素时，才弹堆顶+加入新元素
                        minHeap.poll();
                        minHeap.add(node);
                    }
                }
            }

            // 最后，将门槛堆中的 node 依次弹出，塞入ans数组结果
            int[] ans = new int[k];
            int index = 0;
            while (!minHeap.isEmpty()) {
                ans[index++] = minHeap.poll().val;
            }
            return ans;
        }


        // 【构建Node数组】 包含val + freq两个属性
        public class Node {
            int val;
            int freq;

            public Node(int v, int f) {
                val = v;
                freq = f;
            }
        }

        public class MyComparator implements Comparator<Node> {
            public int compare(Node o1, Node o2) {
                return o1.freq - o2.freq;
            }
        }
    }

}
```

### 易错点

- 2. 最优解是 桶（后面TODO）
- TODO: 这个PriorityQueue的 lambda太屌了。   泛型Integer对应下标（本题是数字本身，对应Map.key），lambda比较时，去map拿value。
- TODO: 这是门槛堆的写法，一定要记住。
- TODO: 小根堆的堆顶是当前 K 个候选中频率最低的元素。当大小超过 K，就把它淘汰。
- 【错误点】这里不是 重新赋值。 而是直接访问对应Node，修改Node里面的值
- PriorityQueue<Node> minHeap = new PriorityQueue<>((a,b) -> (a.freq - b.freq)); // 注意，因为对象不可比较，所以要传入比较器

### 复杂度与面试记忆

- <p>要求算法的时间复杂度优于`O(N log N)`。

> 记忆模板：Top K用大小为K的门槛堆；中位数用左大根堆和右小根堆并维护规模差不超过1。

### 项目文件

- [主实现](Q347_TopKFrequentElements.java)

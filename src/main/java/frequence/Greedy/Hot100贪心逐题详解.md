# Hot 100 贪心逐题详解

> 本文按项目现有package整理。`【Hot 100】`是主线题，`【扩展】`是同模型扩展题。

## 专题核心

贪心每一步做局部最优选择，必须证明该选择不会损失全局最优解。

**统一记忆：** 先写可达范围或边界不变量；不要只凭直觉选最大值，要说明交换论证或分层含义。

## 题目清单

- Q45 JumpGameII `【Hot 100】`
- Q55 JumpGame `【Hot 100】`
- Q763 PartitionLabels `【Hot 100】`

---

## Q45 JumpGameII 【Hot 100】

### 核心思想与题目要求

45. 跳跃游戏 II
给定非负整数数组，每个元素表示从当前位置最多可以向右跳多少步。题目保证最后一个下标
可达，返回到达最后一个下标所需的最少跳跃次数。
当前实现来自LeetCode已保存代码，使用“贪心 + BFS分层”模型：
`currentEnd`是当前跳跃次数能够覆盖的层边界，`farthest`是扫描当前层期间发现的
下一层最远边界。扫描完当前层才增加一次跳跃次数，因此不会把层内每个位置都算作一次跳跃。
时间复杂度`O(N)`，额外空间复杂度`O(1)`。

### 算法流程与正确性依据

1. 根据题意确定输入中可利用的单调性、值域、结构关系或可复用子问题。
2. 使用本专题模型组织状态：先写可达范围或边界不变量；不要只凭直觉选最大值，要说明交换论证或分层含义。
3. 每次更新后维持上述定义；只有在不变量仍成立时才移动指针、覆盖数据或进入下一状态。
4. 最终从定义直接读取答案，而不是根据样例猜测返回位置。

正确性的关键是：每一步只排除已经证明不可能成为答案的选择，或者完整枚举状态定义允许的所有前驱，因此不会漏解。

### 标准 Java 代码

```java
package frequence.Greedy;

/**
 * 45. 跳跃游戏 II
 *
 * <p>给定非负整数数组，每个元素表示从当前位置最多可以向右跳多少步。题目保证最后一个下标
 * 可达，返回到达最后一个下标所需的最少跳跃次数。
 *
 * <p>当前实现来自LeetCode已保存代码，使用“贪心 + BFS分层”模型：
 * {@code currentEnd}是当前跳跃次数能够覆盖的层边界，{@code farthest}是扫描当前层期间发现的
 * 下一层最远边界。扫描完当前层才增加一次跳跃次数，因此不会把层内每个位置都算作一次跳跃。
 *
 * <p>时间复杂度{@code O(N)}，额外空间复杂度{@code O(1)}。
 */
public class Q45_JumpGameII {

    public static class Solution {

        public int jump(int[] nums) {
            // TODO: 【遗漏-错误】长度为1时已经位于终点，不需要跳跃。
            if (nums.length == 1) {
                return 0;
            }

            int jumps = 0;
            int currentEnd = 0;
            int farthest = 0;

            for (int i = 0; i < nums.length; i++) {
                farthest = Math.max(farthest, i + nums[i]);

                if (farthest >= nums.length - 1) {
                    return jumps + 1;
                }

                if (i == currentEnd) {
                    jumps++;
                    currentEnd = farthest;
                }
            }
            return -1;
        }
    }
}
```

### 易错点

- TODO: 【遗漏-错误】长度为1时已经位于终点，不需要跳跃。

### 复杂度与面试记忆

- <p>时间复杂度`O(N)`，额外空间复杂度`O(1)`。

> 记忆模板：先写可达范围或边界不变量；不要只凭直觉选最大值，要说明交换论证或分层含义。

### 项目文件

- [主实现](Q45_JumpGameII.java)

---

## Q55 JumpGame 【Hot 100】

### 核心思想与题目要求

55. 跳跃游戏
给定非负整数数组，每个元素表示从当前位置最多可以向右跳多少步，判断能否到达最后一个下标。
当前实现来自LeetCode已通过提交。`end`表示当前所有可达位置能够继续扩展到的最远
下标。只有`index <= end`时，当前位置才可达；遍历过程中持续扩大`end`，一旦
覆盖最后一个下标即可返回成功。
时间复杂度`O(N)`，额外空间复杂度`O(1)`。

### 算法流程与正确性依据

1. 根据题意确定输入中可利用的单调性、值域、结构关系或可复用子问题。
2. 使用本专题模型组织状态：先写可达范围或边界不变量；不要只凭直觉选最大值，要说明交换论证或分层含义。
3. 每次更新后维持上述定义；只有在不变量仍成立时才移动指针、覆盖数据或进入下一状态。
4. 最终从定义直接读取答案，而不是根据样例猜测返回位置。

正确性的关键是：每一步只排除已经证明不可能成为答案的选择，或者完整枚举状态定义允许的所有前驱，因此不会漏解。

### 标准 Java 代码

```java
package frequence.Greedy;

/**
 * 55. 跳跃游戏
 *
 * <p>给定非负整数数组，每个元素表示从当前位置最多可以向右跳多少步，判断能否到达最后一个下标。
 *
 * <p>当前实现来自LeetCode已通过提交。{@code end}表示当前所有可达位置能够继续扩展到的最远
 * 下标。只有{@code index <= end}时，当前位置才可达；遍历过程中持续扩大{@code end}，一旦
 * 覆盖最后一个下标即可返回成功。
 *
 * <p>时间复杂度{@code O(N)}，额外空间复杂度{@code O(1)}。
 */
public class Q55_JumpGame {

    public static class Solution {

        public boolean canJump(int[] nums) {
            int end = nums[0];
            int index = 0;
            while (index <= end) {
                end = Math.max(end, index + nums[index]);
                if (end >= nums.length - 1) {
                    return true;
                }

                // TODO: 【错误-遗漏】while循环末尾不要忘记推进循环变量。
                index++;
            }
            return false;
        }
    }
}
```

### 易错点

- TODO: 【错误-遗漏】while循环末尾不要忘记推进循环变量。

### 复杂度与面试记忆

- <p>时间复杂度`O(N)`，额外空间复杂度`O(1)`。

> 记忆模板：先写可达范围或边界不变量；不要只凭直觉选最大值，要说明交换论证或分层含义。

### 项目文件

- [主实现](Q55_JumpGame.java)

---

## Q763 PartitionLabels 【Hot 100】

### 核心思想与题目要求

763. 划分字母区间
把字符串划分为尽可能多的片段，使每个字母最多出现在一个片段中，返回每个片段的长度。
当前实现来自LeetCode已保存代码。`count`维护每个字母在尚未扫描部分中的剩余次数，
`set`记录当前及历史片段已经出现过的字母。当集合中所有字母剩余次数均为0时，当前
位置就是一个合法且最早的切分点。
题目字符集固定为26个小写字母，`isClear`每次最多检查26项，所以总体时间复杂度
仍为`O(N)`，额外空间复杂度为`O(1)`。

### 算法流程与正确性依据

1. 根据题意确定输入中可利用的单调性、值域、结构关系或可复用子问题。
2. 使用本专题模型组织状态：先写可达范围或边界不变量；不要只凭直觉选最大值，要说明交换论证或分层含义。
3. 每次更新后维持上述定义；只有在不变量仍成立时才移动指针、覆盖数据或进入下一状态。
4. 最终从定义直接读取答案，而不是根据样例猜测返回位置。

正确性的关键是：每一步只排除已经证明不可能成为答案的选择，或者完整枚举状态定义允许的所有前驱，因此不会漏解。

### 标准 Java 代码

```java
package frequence.Greedy;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * 763. 划分字母区间
 *
 * <p>把字符串划分为尽可能多的片段，使每个字母最多出现在一个片段中，返回每个片段的长度。
 *
 * <p>当前实现来自LeetCode已保存代码。{@code count}维护每个字母在尚未扫描部分中的剩余次数，
 * {@code set}记录当前及历史片段已经出现过的字母。当集合中所有字母剩余次数均为0时，当前
 * 位置就是一个合法且最早的切分点。
 *
 * <p>题目字符集固定为26个小写字母，{@code isClear}每次最多检查26项，所以总体时间复杂度
 * 仍为{@code O(N)}，额外空间复杂度为{@code O(1)}。
 */
public class Q763_PartitionLabels {

    public static class Solution {

        public List<Integer> partitionLabels(String s) {
            int[] count = new int[26];
            char[] chars = s.toCharArray();
            for (char current : chars) {
                count[current - 'a']++;
            }

            int letters = 0;
            Set<Character> set = new HashSet<>();
            List<Integer> ans = new ArrayList<>();
            for (char current : chars) {
                letters++;
                set.add(current);
                count[current - 'a']--;
                if (isClear(set, count)) {
                    ans.add(letters);
                    letters = 0;
                }
            }
            return ans;
        }

        public boolean isClear(Set<Character> set, int[] count) {
            // TODO: Set直接使用增强for遍历；Map才使用keySet()或entrySet()。
            for (char current : set) {
                if (count[current - 'a'] != 0) {
                    return false;
                }
            }
            return true;
        }
    }
}
```

### 易错点

- TODO: Set直接使用增强for遍历；Map才使用keySet()或entrySet()。

### 复杂度与面试记忆

- <p>题目字符集固定为26个小写字母，`isClear`每次最多检查26项，所以总体时间复杂度
- 仍为`O(N)`，额外空间复杂度为`O(1)`。

> 记忆模板：先写可达范围或边界不变量；不要只凭直觉选最大值，要说明交换论证或分层含义。

### 项目文件

- [主实现](Q763_PartitionLabels.java)

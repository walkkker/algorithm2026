# 子串与子序列专题

## 一、先区分三个概念

| 概念 | 是否连续 | 是否保持相对顺序 | 长度为N时的候选规模 |
|---|---|---|---|
| 子串 substring | 必须连续 | 自动保持 | `N(N+1)/2`个位置区间 |
| 子数组 subarray | 必须连续 | 自动保持 | `N(N+1)/2`个位置区间 |
| 子序列 subsequence | 不要求连续 | 必须保持 | 最多`2^N`种下标选择 |

子串和子数组的区别仅在输入载体：字符串中的连续段叫子串，数组中的连续段叫子数组。子序列允许跳过
中间元素，因此每个位置都可能出现“选或不选”的组合决策。

## 二、为什么子串较少使用DP

子串一旦确定左右边界`[left,right]`，中间内容随之唯一确定；连续性通常还能形成可增量维护的窗口，
所以优先考虑：

1. 滑动窗口：窗口条件在扩张/收缩时可以增量维护。
2. 前缀和/前缀计数：区间值可以由两个前缀状态相减。
3. 中心扩展、Manacher：利用回文的中心对称。
4. KMP、Z Algorithm、滚动哈希：处理模式匹配。
5. 结尾型DP：当前连续答案只能连接`i-1`时使用。

子串不是不能使用DP。Q5可以定义`dp[left][right]`表示区间是否回文；Q32可以定义`dp[i]`
表示必须以i结尾的最长有效括号长度。但如果存在更直接的窗口、中心或字符串专用算法，应当比较复杂度。

### 当前子串题目索引

| 题目 | 核心模型 | 代码 |
|---|---|---|
| 枚举所有子串 | 枚举所有连续区间`[left,right)` | [AllSubstrings](../Backtracking/AllSubstrings.java) |
| Q3 最长无重复子串 | 可变长度滑动窗口 | [Q3](../双指针/SlidingWindow/Q3_LongestSubstringWithoutRepeatingCharacters.java) |
| Q438 找到所有字母异位词 | 固定长度滑动窗口+词频 | [Q438](../双指针/SlidingWindow/Q438.java) |
| Q76 最小覆盖子串 | 可变长度滑动窗口；每个right寻找最短合法窗口 | [Q76](../双指针/SlidingWindow/Q76.java) |
| Q5 最长回文子串 | 区间DP、中心扩展或Manacher | [Q5](../dp/multidimensional/Q5_LongestPalindromicSubstring.java) |
| Q32 最长有效括号 | 必须以i结尾的一维DP，或栈 | [Q32](../dp/Q32_LongestValidParentheses.java) |
| Q139 单词拆分 | 前缀切分DP，枚举最后一个合法子串 | [Q139](../dp/Q139_WordBreak.java) |
| Q131 分割回文串 | 回溯枚举切分点+回文区间判断 | [Q131](../Backtracking/Q131_PalindromePartitioning.java) |

## 三、为什么子序列经常使用DP

子序列不要求连续。求最优值或方案数量时，每个位置通常有“选择、跳过”两种行为，暴力枚举规模可达
`2^N`。DP通过保存前缀、结尾位置或左右区间的答案，合并重复子问题。

但“子序列一定使用DP”是错误结论：如果只判断一个固定序列能否按顺序匹配，双指针即可；LIS还可以
利用单调性使用贪心+二分优化。

### 当前子序列题目索引

| 题目 | 核心模型 | DP状态值语义 | 代码 |
|---|---|---|---|
| Q392 判断子序列 | 双指针顺序匹配 | 不使用DP | [Q392](./Q392_IsSubsequence.java) |
| Q300 最长递增子序列 | 一维位置结尾型DP；可用贪心+二分优化 | 最优长度 | [Q300](../dp/Q300_LongestIncreasingSubsequence.java) |
| Q673 最长递增子序列数量 | 一维位置结尾型DP | 最优长度+最优方案计数 | [Q673](../dp/Q673_NumberOfLongestIncreasingSubsequence.java) |
| Q1143 最长公共子序列 | 双序列前缀DP | 最优长度 | [Q1143](../dp/multidimensional/Q1143_LongestCommonSubsequence.java) |
| Q115 不同的子序列 | 双序列前缀DP | 所有合法方案计数 | [Q115](../dp/multidimensional/Q115_DistinctSubsequences.java) |
| Q516 最长回文子序列 | 单序列区间DP | 最优长度 | [PalindromeSubsequence](../../dp/PalindromeSubsequence.java) |
| Q78 子集 | 每个位置选/不选的回溯生成 | 枚举全部结果，不是最优值DP | [Q78](../Backtracking/Q78_Subsets.java) |

## 四、Q115与Q673的计数区别

### Q115：纯计数DP

```text
dp[i][j] = s前i个字符组成t前j个字符的全部方案数
```

不存在最优值比较，所有合法来源直接相加：

```text
字符不同：dp[i][j] = dp[i-1][j]
字符相同：dp[i][j] = dp[i-1][j] + dp[i-1][j-1]
```

### Q673：最优值+最优方案计数

```text
length[i] = 必须以nums[i]结尾的最优长度
count[i]  = 达到length[i]的方案数量
```

```text
发现更长候选：覆盖length[i]，同时用count[j]覆盖count[i]
发现同长候选：保留length[i]，把count[j]累加到count[i]
发现更短候选：忽略
```

`count[i]`是状态i的第二个属性，不是新的独立状态参数。状态仍然只由结尾位置i确定。

## 五、子数组模型对照

子数组与子串一样要求连续，所以常见算法结构也相似：

| 问题 | 常用模型 | 代码 |
|---|---|---|
| Q53 最大子数组和 | 前缀和减左侧最小前缀，或Kadane结尾型DP | [Q53](../array/Q53.java) |
| Q152 乘积最大子数组 | 必须以i结尾的最大/最小双状态DP | [Q152](../dp/Q152_MaximumProductSubarray.java) |
| 正数数组中累加和等于K的最长子数组 | 滑动窗口 | [LongestSumSubArrayLengthInPositiveArray](../../子数组累加和长度三连/mine/LongestSumSubArrayLengthInPositiveArray.java) |
| 含负数数组中累加和等于K的最长子数组 | 前缀和+最早位置HashMap | [LongestSumSubArrayLength](../../子数组累加和长度三连/mine/LongestSumSubArrayLength.java) |
| 累加和小于等于K的最长子数组 | 最小累加块+扩展窗口 | [LongestLessSumSubArray](../../子数组累加和长度三连/mine/LongestLessSumSubArray.java) |

数组包含负数后，窗口和通常失去单调性，不能机械使用滑动窗口；应改用前缀和、HashMap、单调结构或
预处理最小累加块。

## 六、面试判断流程

```text
1. 是否要求连续？
   是：优先窗口、前缀、中心扩展、单调结构；再考虑连续结尾型DP。
   否：进入子序列模型。

2. 子序列是否只做一次顺序匹配？
   是：双指针。
   否：继续分析前缀、结尾位置或区间状态。

3. DP格子保存什么？
   能否完成：boolean可行性。
   最大/最小：最优值DP。
   所有合法方案：纯计数DP。
   最优方案数量：最优值+计数。

4. 最终答案在哪里？
   前缀型通常在dp[n]或dp[m][n]；
   必须以i结尾型通常需要扫描全部i；
   区间型整段答案通常在dp[0][n-1]。
```

## 七、核心结论

> 连续性减少了边界选择自由度，因此子串和子数组经常存在窗口、前缀或中心类算法；子序列允许跳跃
> 选择，优化和计数问题更容易形成DP。但算法选择最终取决于问题目标，不应只根据“子串/子序列”名称
> 机械套模板。

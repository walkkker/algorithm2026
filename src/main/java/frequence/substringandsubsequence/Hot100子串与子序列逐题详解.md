# Hot 100 子串与子序列逐题详解

> 本文按项目现有package整理。`【Hot 100】`是主线题，`【扩展】`是同模型扩展题。

## 专题核心

子串要求连续，子序列只保持相对顺序；二者决定滑动窗口、前缀和或DP。

**统一记忆：** 先判断是否连续；单次子序列匹配用双指针，多次查询可预处理位置，多序列最优问题使用DP。

## 题目清单

> 本专题当前题目均为Hot 100之外的扩展题，用于补全同类模型。

- Q392 IsSubsequence `【扩展】`

---

## Q392 IsSubsequence 【扩展】

### 核心思想与题目要求

392. 判断子序列
**DONE: 【已独立完成】**当前实现已通过固定用例验证。
状态总表参见`frequence/待独立完成题目清单.md`。
给定字符串s和t，判断s是否为t的子序列。子序列不要求连续，但必须保持原字符相对顺序。
**算法类型：**双指针，不需要DP。因为本题只判断一个固定序列能否按顺序匹配，不存在
“求最长、求最少、统计所有方案”等需要保存重叠子问题答案的目标。指针sourceIndex只在匹配成功
时前进，targetIndex始终向右扫描，最终检查s是否全部匹配。
时间复杂度O(|T|)，额外空间O(1)。相关模型参见同目录《子串与子序列区别.md》。

TODO: 【一开始的错误】你的两个指针职责写反了。  题目是：s是否是t的子序列。  我写成了t是否是s的子序列。

### 算法流程与正确性依据

1. 根据题意确定输入中可利用的单调性、值域、结构关系或可复用子问题。
2. 使用本专题模型组织状态：先判断是否连续；单次子序列匹配用双指针，多次查询可预处理位置，多序列最优问题使用DP。
3. 每次更新后维持上述定义；只有在不变量仍成立时才移动指针、覆盖数据或进入下一状态。
4. 最终从定义直接读取答案，而不是根据样例猜测返回位置。

正确性的关键是：每一步只排除已经证明不可能成为答案的选择，或者完整枚举状态定义允许的所有前驱，因此不会漏解。

### 标准 Java 代码

```java
package frequence.substringandsubsequence;

/**
 * 392. 判断子序列
 *
 * <p><b>DONE: 【已独立完成】</b>当前实现已通过固定用例验证。
 * 状态总表参见{@code frequence/待独立完成题目清单.md}。
 *
 * <p>给定字符串s和t，判断s是否为t的子序列。子序列不要求连续，但必须保持原字符相对顺序。
 *
 * <p><b>算法类型：</b>双指针，不需要DP。因为本题只判断一个固定序列能否按顺序匹配，不存在
 * “求最长、求最少、统计所有方案”等需要保存重叠子问题答案的目标。指针sourceIndex只在匹配成功
 * 时前进，targetIndex始终向右扫描，最终检查s是否全部匹配。
 *
 * <p>时间复杂度O(|T|)，额外空间O(1)。相关模型参见同目录《子串与子序列区别.md》。
 */
public class Q392_IsSubsequence {

    /**
     * TODO: 【一开始的错误】你的两个指针职责写反了。  题目是：s是否是t的子序列。  我写成了t是否是s的子序列。
     * @param s
     * @param t
     * @return
     */

    public boolean myIsSubsequence(String s, String t) {
        char[] source = s.toCharArray();
        char[] target = t.toCharArray();
        int p1 = source.length - 1;
        int p2 = target.length - 1;
        while (p1 >= 0 && p2 >= 0) {
            if (source[p1] == target[p2]) {
                p1--;
                p2--;
            } else {
                p2--;
            }
        }
        return p1 == -1;
    }





        public boolean isSubsequence(String s, String t) {
        char[] source = s.toCharArray();
        char[] target = t.toCharArray();
        int sourceIndex = 0;

        for (int targetIndex = 0;
             targetIndex < target.length && sourceIndex < source.length;
             targetIndex++) {
            if (source[sourceIndex] == target[targetIndex]) {
                sourceIndex++;
            }
        }
        return sourceIndex == source.length;
    }
}
```

### 易错点

- TODO: 【一开始的错误】你的两个指针职责写反了。  题目是：s是否是t的子序列。  我写成了t是否是s的子序列。

### 复杂度与面试记忆

- <p>时间复杂度O(|T|)，额外空间O(1)。相关模型参见同目录《子串与子序列区别.md》。

> 记忆模板：先判断是否连续；单次子序列匹配用双指针，多次查询可预处理位置，多序列最优问题使用DP。

### 项目文件

- [主实现](Q392_IsSubsequence.java)
